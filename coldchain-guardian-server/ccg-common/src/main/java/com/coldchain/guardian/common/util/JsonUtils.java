package com.coldchain.guardian.common.util;

public class JsonUtils {

    // This would normally contain methods for JSON serialization/deserialization
    // For example, using Jackson or Gson

    /**
     * Convert object to JSON string
     */
    public static String toJson(Object obj) {
        // Implementation would depend on the JSON library used
        return "";
    }

    /**
     * Convert JSON string to object
     */
    public static <T> T fromJson(String json, Class<T> clazz) {
        // Implementation would depend on the JSON library used
        return null;
    }
}